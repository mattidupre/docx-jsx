import type { PrefixesConfig } from '../entities';

export const createMockPrefixesConfig = (): PrefixesConfig => ({
  elementClassName: 'mock-element-name',
  variantClassName: 'mock-class-name',
  cssVariable: 'mock-variable',
});
